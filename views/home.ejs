
<%-include("partials/header") -%>

<h1>Home</h1>

<p><%= startingContent %></p>

<!--The first line runs a loop so that more than one posts can be printed the second and the third line
render the title and the content(upto the first 100 words)and the third line provides a link to read
the rest of the file and leads the user to a new page.-->

<% posts.forEach(function(post){ %>
    <h1><%=post.title%></h1>
    <p><%=post.content.substring(0,100)+ " ..."%></p>
    <a href="/posts/<%=post._id%>">Read More</a>
<%  })   %>

<%-include("partials/footer")-%>